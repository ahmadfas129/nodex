NodeX — Meme coin marketing site (Next.js)

Quick start:

```bash
# install deps
npm install
# dev server
npm run dev
```

Replace `public/coin.svg` with your pasted coin image (name it `coin.svg` or `coin.png`).
